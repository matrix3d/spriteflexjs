/**
 * Generated by Apache Royale Compiler from flash/geom/Point.as
 * flash.geom.Point
 *
 * @fileoverview
 *
 * @suppress {missingRequire|checkTypes|accessControls}
 */

goog.provide('flash.geom.Point');
/* Royale Dependency List: */



/**
 * @constructor
 * @param {number=} x
 * @param {number=} y
 */
flash.geom.Point = function(x, y) {
  x = typeof x !== 'undefined' ? x : 0;
  y = typeof y !== 'undefined' ? y : 0;
  ;
  this.x = x;
  this.y = y;
};


/**
 * @type {number}
 */
flash.geom.Point.prototype.x = NaN;


/**
 * @type {number}
 */
flash.geom.Point.prototype.y = NaN;


/**
 * @nocollapse
 * @param {flash.geom.Point} pt1
 * @param {flash.geom.Point} pt2
 * @param {number} f
 * @return {flash.geom.Point}
 */
flash.geom.Point.interpolate = function(pt1, pt2, f) {
  return new flash.geom.Point(pt2.x + f * (pt1.x - pt2.x), pt2.y + f * (pt1.y - pt2.y));
};


/**
 * @nocollapse
 * @param {flash.geom.Point} pt1
 * @param {flash.geom.Point} pt2
 * @return {number}
 */
flash.geom.Point.distance = function(pt1, pt2) {
  return pt1.subtract(pt2).length;
};


/**
 * @nocollapse
 * @param {number} len
 * @param {number} angle
 * @return {flash.geom.Point}
 */
flash.geom.Point.polar = function(len, angle) {
  return new flash.geom.Point(len * Math.cos(angle), len * Math.sin(angle));
};


/**
 * @return {flash.geom.Point}
 */
flash.geom.Point.prototype.clone = function() {
  return new flash.geom.Point(this.x, this.y);
};


/**
 * @param {number} dx
 * @param {number} dy
 */
flash.geom.Point.prototype.offset = function(dx, dy) {
  this.x = this.x + dx;
  this.y = this.y + dy;
};


/**
 * @param {flash.geom.Point} toCompare
 * @return {boolean}
 */
flash.geom.Point.prototype.equals = function(toCompare) {
  return toCompare.x === this.x && toCompare.y === this.y;
};


/**
 * @param {flash.geom.Point} v
 * @return {flash.geom.Point}
 */
flash.geom.Point.prototype.subtract = function(v) {
  return new flash.geom.Point(this.x - v.x, this.y - v.y);
};


/**
 * @param {flash.geom.Point} v
 * @return {flash.geom.Point}
 */
flash.geom.Point.prototype.add = function(v) {
  return new flash.geom.Point(this.x + v.x, this.y + v.y);
};


/**
 * @param {number} thickness
 */
flash.geom.Point.prototype.normalize = function(thickness) {
  var /** @type {number} */ invD = this.length;
  if (invD > 0) {
    invD = thickness / invD;
    this.x = this.x * invD;
    this.y = this.y * invD;
  }
};


/**
 * @return {string}
 */
flash.geom.Point.prototype.toString = function() {
  return "(x=" + this.x + ", y=" + this.y + ")";
};


/**
 * @param {flash.geom.Point} sourcePoint
 */
flash.geom.Point.prototype.copyFrom = function(sourcePoint) {
  this.x = sourcePoint.x;
  this.y = sourcePoint.y;
};


/**
 * @param {number} xa
 * @param {number} ya
 */
flash.geom.Point.prototype.setTo = function(xa, ya) {
  this.x = xa;
  this.y = ya;
};


flash.geom.Point.prototype.get__length = function() {
  return Math.sqrt(this.x * this.x + this.y * this.y);
};


Object.defineProperties(flash.geom.Point.prototype, /** @lends {flash.geom.Point.prototype} */ {
/**
 * @type {number}
 */
length: {
get: flash.geom.Point.prototype.get__length}}
);


/**
 * Metadata
 *
 * @type {Object.<string, Array.<Object>>}
 */
flash.geom.Point.prototype.ROYALE_CLASS_INFO = { names: [{ name: 'Point', qName: 'flash.geom.Point', kind: 'class' }] };



/**
 * Reflection
 *
 * @return {Object.<string, Function>}
 */
flash.geom.Point.prototype.ROYALE_REFLECTION_INFO = function () {
  return {
    variables: function () {
      return {
        'x': { type: 'Number', get_set: function (/** flash.geom.Point */ inst, /** * */ v) {return v !== undefined ? inst.x = v : inst.x;}},
        'y': { type: 'Number', get_set: function (/** flash.geom.Point */ inst, /** * */ v) {return v !== undefined ? inst.y = v : inst.y;}}
      };
    },
    accessors: function () {
      return {
        'length': { type: 'Number', access: 'readonly', declaredBy: 'flash.geom.Point'}
      };
    },
    methods: function () {
      return {
        'Point': { type: '', declaredBy: 'flash.geom.Point', parameters: function () { return [ 'Number', true ,'Number', true ]; }},
        '|interpolate': { type: 'flash.geom.Point', declaredBy: 'flash.geom.Point', parameters: function () { return [ 'flash.geom.Point', false ,'flash.geom.Point', false ,'Number', false ]; }},
        '|distance': { type: 'Number', declaredBy: 'flash.geom.Point', parameters: function () { return [ 'flash.geom.Point', false ,'flash.geom.Point', false ]; }},
        '|polar': { type: 'flash.geom.Point', declaredBy: 'flash.geom.Point', parameters: function () { return [ 'Number', false ,'Number', false ]; }},
        'clone': { type: 'flash.geom.Point', declaredBy: 'flash.geom.Point'},
        'offset': { type: 'void', declaredBy: 'flash.geom.Point', parameters: function () { return [ 'Number', false ,'Number', false ]; }},
        'equals': { type: 'Boolean', declaredBy: 'flash.geom.Point', parameters: function () { return [ 'flash.geom.Point', false ]; }},
        'subtract': { type: 'flash.geom.Point', declaredBy: 'flash.geom.Point', parameters: function () { return [ 'flash.geom.Point', false ]; }},
        'add': { type: 'flash.geom.Point', declaredBy: 'flash.geom.Point', parameters: function () { return [ 'flash.geom.Point', false ]; }},
        'normalize': { type: 'void', declaredBy: 'flash.geom.Point', parameters: function () { return [ 'Number', false ]; }},
        'toString': { type: 'String', declaredBy: 'flash.geom.Point'},
        'copyFrom': { type: 'void', declaredBy: 'flash.geom.Point', parameters: function () { return [ 'flash.geom.Point', false ]; }},
        'setTo': { type: 'void', declaredBy: 'flash.geom.Point', parameters: function () { return [ 'Number', false ,'Number', false ]; }}
      };
    }
  };
};
/**
 * @const
 * @type {number}
 */
flash.geom.Point.prototype.ROYALE_COMPILE_FLAGS = 9;
/**
 * Provide reflection support for distinguishing dynamic fields on class object (static)
 * @const
 * @type {Array<string>}
 */
flash.geom.Point.prototype.ROYALE_INITIAL_STATICS = Object.keys(flash.geom.Point);
